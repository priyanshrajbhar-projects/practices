[build]
  # 'base' directory wahi rakho
  base = "video_chat"
  
  # YEH HAI FIX:
  # Publish directory ko 'dist' se '.' (current directory) kar do
  # Aur 'command' ko poori tarah hata do
  publish = "." 
  # command = "npm run build"  <-- YEH HATA DIYA

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200